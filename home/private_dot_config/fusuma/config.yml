swipe:
  3:
    left:
      sendkey: "bspc desktop -f prev"
    right:
      sendkey: "bspc desktop -f next"
    up:
      sendkey: "LEFTMETA+ENTER"
    down:
      sendkey: "LEFTMETA+SPACE"
  4:
    left:
      sendkey: "bspc desktop -f prev"
    right:
      sendkey: "LEFTALT+RIGHT"
    up:
      command: "bspc node -t 'fullscreen'"
    down:
      command: "bspc node -t 'tiled'"
pinch:
  in:
    sendkey: "LEFTCTRL+KPMINUS"
  out:
    sendkey: "LEFTCTRL+KPPLUS"
hold:
  3:
    sendkey: "LEFTMETA+SPACE"
  2:
    sendkey: "LEFTMETA+SPACE"
  4:
    sendkey: "LEFTMETA+W"

threshold:
  swipe: 0.6

interval:
  swipe: 0.8
